import { useEffect } from 'react';

const LoginPage = () => {
  useEffect(() => {
    // login logic with redirect ??????
  }, []);

  return (
    <div>
      <p>login page mock</p>
    </div>
  );
};

export { LoginPage };
